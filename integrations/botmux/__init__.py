"""BotMux integration for LearningFlow.

Use ``integrations.botmux.bridge`` for the runner entry point. Imports stay
lazy so ``python -m integrations.botmux.bridge`` starts without runpy warnings.
"""
