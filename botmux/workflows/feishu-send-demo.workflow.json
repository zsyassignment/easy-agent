{
  "workflowId": "feishu-send-demo",
  "version": 1,
  "defaults": {
    "retryPolicy": { "maxAttempts": 1, "backoff": "fixed", "baseMs": 1000 },
    "timeoutMs": 60000,
    "maxOutputBytes": 4096
  },
  "nodes": {
    "send-text": {
      "type": "hostExecutor",
      "executor": "feishu-send",
      "description": "Demo of the bare feishu-send executor mechanics (idempotent submit + sidecar persistence). Production workflows should gate this with humanGate.stage='before' so an operator approves the content; see canary-multistep.workflow.json for the canonical draft→confirm→send pattern. `unsafeAllowUngated` is the explicit opt-out — never an oversight.",
      "unsafeAllowUngated": true,
      "input": {
        "larkAppId": "cli_demo_PLACEHOLDER",
        "chatId": "oc_demo_PLACEHOLDER",
        "content": "feishu-send-demo: hello from workflow A2",
        "msgType": "text"
      }
    }
  }
}
